{
  "buildCommand": "cd client && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "dist/public",
  "framework": null,
  "installCommand": "pnpm install --no-frozen-lockfile"
}
